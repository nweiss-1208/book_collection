<p style="color: green"><%= notice %></p>

<% content_for :title, "Show Book" %>

<h1>Book Details</h1>

<%= render @book %>

<div>
  <%= link_to "Update", edit_book_path(@book) %> |
  <%= link_to "Delete", delete_book_path(@book) %> |
  <%= link_to "Back to Home", books_path %>
</div>
